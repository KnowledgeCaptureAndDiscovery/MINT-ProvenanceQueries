#+ method: GET
#+ summary: TO DO - gets all the activities of an execution.
#+ defaults:
#+   - exec:TO ADD

PREFIX opmw: <http://www.opmw.org/ontology/>

SELECT ?execution ?status from <urn:x-arq:UnionGraph>
WHERE {
  #TO DO
}
